// Sales seed data: EMPTY. The Quick Sale log starts with no entries
// everywhere (dev store, in-browser demo, and production). The DEMO sales
// that used to live here were removed before launch (see git history).

export function seedSalesStore() {
  return [];
}
