// Overview: the admin landing page. It summarizes the catalog and the Quick
// Sale log into at-a-glance statistics. Built out in a later step; this stub
// keeps the three-page shell working.

export function OverviewPage() {
  return (
    <div className="admin-placeholder">
      <p>Overview statistics load here.</p>
    </div>
  );
}
