// Quick Sale: the counter sales-logging tool. Staff search a product, log the
// sale at its listed price, and optionally mark the item Sold live. Built out
// in a later step; this stub keeps the three-page shell working.

export function QuickSalePage() {
  return (
    <div className="admin-placeholder">
      <p>Quick Sale tool loads here.</p>
    </div>
  );
}
